MONGODB_URI=
PORT=4000
CORS_ORIGIN=http://localhost:3000
NODE_ENV=development

# Vercel Blob read-write token for instructor student-file uploads
# (private store). Optional — without it uploads/downloads return 503.
BLOB_READ_WRITE_TOKEN=
